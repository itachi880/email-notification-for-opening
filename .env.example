# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-random-secret-key-here

# Next.js Configuration  
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Database Configuration
# For development (SQLite)
DATABASE_URL="file:./dev.db"

# For production (PostgreSQL) - uncomment and set your database URL
# DATABASE_URL="postgresql://username:password@hostname:port/database?schema=public"

# Production Environment Variables (set these in your deployment platform)
# NEXTAUTH_URL=https://your-domain.vercel.app
# NEXTAUTH_SECRET=your-strong-production-secret
# NEXT_PUBLIC_BASE_URL=https://your-domain.vercel.app
# DATABASE_URL=your-production-database-url

# Note: Gmail credentials are now managed through the login system
# Users will enter their Gmail address and App Password when signing in
